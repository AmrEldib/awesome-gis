<ul class="list-unstyled">
    {{#each contributors}}
        {{#if url}}
            <li><a href="{{url}}">{{name}}</a></li>
        {{else}}
            <li>{{name}}</li>
        {{/if}}
    {{/each}}
</ul>